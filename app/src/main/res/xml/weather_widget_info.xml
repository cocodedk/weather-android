<?xml version="1.0" encoding="utf-8"?>
<!-- No configure activity: the widget follows whichever location is selected in
     the app, which keeps the two in step with nothing for the user to set up.

     updatePeriodMillis is 30 minutes, the platform minimum. Open-Meteo publishes
     hourly, so anything shorter would spend battery re-fetching the same numbers. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:initialLayout="@layout/widget_weather"
    android:description="@string/widget_description"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:targetCellWidth="3"
    android:targetCellHeight="2"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewLayout="@layout/widget_weather"
    android:updatePeriodMillis="1800000" />
